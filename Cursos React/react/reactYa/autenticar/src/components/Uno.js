import React, { Fragment } from 'react';

const Uno = () => {

    //const [state, setstate] = useState(initialState);

    return (

        <Fragment>
            uno
        </Fragment>

    );

};

export default Uno;