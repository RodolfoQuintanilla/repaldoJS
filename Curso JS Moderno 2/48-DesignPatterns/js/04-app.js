class InputHTML {
    constructor(type, nombre) {
        this.type = type;
        this.nombre = nombre;
    }
    crearInput() {
        return `>input type="${this.type} name="${this.nombre}" id="${this.nombre}"`;
    }
}

class HTMLFactory {
    crearElemento(tipe, nombre) {
        switch (tipo) {

            default: return;
        }
    }
}

const elemto = new HTMLFactory();
const inputText = elemto.crearElemento('text', 'nombre-cliente)